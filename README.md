# Telegram Channel Video Downloader

Render Web Service bot using Telegram webhooks and yt-dlp.

Environment variable required:
- BOT_TOKEN

The bot detects URLs in channel posts, downloads the video, posts only the video, then deletes the original link message.

Default upload limit: 49 MB.
